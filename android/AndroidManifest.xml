<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.mkwpc.quest"
    android:versionCode="1"
    android:versionName="0.1">

    <uses-sdk android:minSdkVersion="29" android:targetSdkVersion="34" />

    <!-- Deliberately NOT declared, and that is the whole point of this app:
         no android.hardware.vr.headtracking, no com.oculus.intent.category.VR.
         Horizon OS then runs this as an ordinary flat 2D panel in the shell
         instead of taking over the display as an immersive OpenXR app. -->

    <uses-feature android:name="android.hardware.gamepad" android:required="false" />
    <uses-feature android:glEsVersion="0x00030002" android:required="false" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Retro WFC online play: the runtime opens plain sockets for the game. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- The runtime reads a ~2.6 GB extracted disc plus a ~2 GB mod overlay
         from a plain directory. Android 11 closed /sdcard/Android/data/<pkg>
         to adb for writing, so that is the only place both the host and the
         app can reach. Granted from the host with
         "appops set <pkg> MANAGE_EXTERNAL_STORAGE allow"; it is a sideload
         permission, not something a store build would ask for. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <application
        android:label="@string/app_name"
        android:allowBackup="false"
        android:debuggable="true"
        android:hasCode="true"
        android:extractNativeLibs="true"
        android:resizeableActivity="true">

        <!-- Horizon OS reads this to decide whether the app may run as a panel
             on a given headset. -->
        <meta-data android:name="com.oculus.supportedDevices"
            android:value="quest2|questpro|quest3|quest3s" />

        <activity
            android:name=".MkwActivity"
            android:label="@string/app_name"
            android:exported="true"
            android:launchMode="singleTask"
            android:resizeableActivity="true"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenSize|screenLayout|uiMode|density">
            <!-- Horizon OS sizes the panel from this element. Without it the
                 app got a 400x640 dp portrait panel (500x800 px at the panel's
                 200 dpi). 1024x576 dp is 1280x720 px, 16:9 like the game's
                 widescreen output; window_width/height in Config.toml match
                 it so the SDL window fills the panel. resizeableActivity lets
                 the user drag the panel to another size. -->
            <layout android:defaultWidth="1024dp"
                android:defaultHeight="576dp"
                android:minWidth="640dp"
                android:minHeight="360dp" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
